package com.example.localmarkettracker.utils;

import android.content.Context;
import com.google.android.gms.maps.GoogleMap;

// NOTE: The GoogleMap import will only resolve after you sync Gradle with the maps dependency added.
public class MapUtils {

    /**
     * Placeholder method to satisfy the MapActivity compilation error.
     * Actual implementation would fetch market data and add markers to the map.
     */
    public static void addAllMarketsToMap(GoogleMap map, Context context) {
        // Log.d("MapUtils", "Placeholder: Adding markets to map.");
        // Implement Firebase query and marker placement here later.
    }
}